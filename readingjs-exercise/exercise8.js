// Take this deeply nested reference type and write the code to find the following values:
//
// The email of user 1.

// First name the object as "obj".

obj["users"][0]["emails"]; // return "chris@hipchat.com"

// The title of user 5.

obj["users"][2]["title"]; // return "Co-founder"

// The user id of the first user in the users array.

obj["users"][0]["user_id"]; // return 1
